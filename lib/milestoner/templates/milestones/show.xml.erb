<section>
  <p style="display: flex; align-items: center; justify-content:center;">
    <img src="<%= project_uri_logo %>" alt="Logo" height=100 width=100>
  </p>

  <% if commits.empty? %>
    <p>All is quiet.</p>
  <% else %>
    <% commits.each do |commit| %>
      <%= commit.render :commit %>
    <% end %>
  <% end %>

  <br/>
  <hr/>

  <p>
    <strong>
      <span><%= total_commits %>.</span>
      <span><%= total_files %>.</span>
      <span><%= total_deletions %>.</span>
      <span><%= total_insertions %>.</span>
    </strong>
  </p>
</section>
